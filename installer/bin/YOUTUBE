#!/usr/bin/env bash
# Development shim for the interactive TUI when run straight from the repo.
# Installed globally by `installer install` at ~/.local/bin/YOUTUBE.
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$DIR"
exec python3 tui.py "$@"
